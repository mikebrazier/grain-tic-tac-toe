in one terminal window, launch the nextjs dev server
`npm run nextdev`

in a separate terminal window, launch the game app with proxy server
`ts-node src/FrontendProxy/runFrontendProxy.ts`

visit localhost in a browser:
    `http://localhost:81/`